{
  "name": "strategic-football",
  "version": "1.0.0",
  "description": "Two-player strategic football game (stick stretch & pass).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "web:build": "node scripts/build-web.js",
    "cap:sync": "npm run web:build && cap sync android",
    "android:build": "npm run cap:sync && gradlew --project-dir android clean bundleRelease assembleRelease",
    "icon:gen": "node scripts/gen-icon.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.2",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.2"
  }
}
